name: solixpost

# Compose de produção. Variáveis de ambiente vêm do arquivo .env na mesma pasta.
#
# Domínio público: https://app.solixpost.com.br (frontend) / https://api.solixpost.com.br (API)
#
# Conexões internas (app → banco, redis, minio) usam os nomes dos containers:
#   DATABASE_URL   = postgresql://USER:PASS@solixpost-postgres:5432/DB?schema=public
#   REDIS_HOST     = solixpost-redis
#   MINIO_ENDPOINT = solixpost-minio
#
# Apenas a porta da aplicação (PORT) é exposta para o host/proxy reverso.

services:
  # ─── Aplicação ──────────────────────────────────────────────────────────────
  solixpost-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: solixpost-api
    container_name: solixpost-api
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    env_file:
      - .env
    depends_on:
      solixpost-postgres:
        condition: service_healthy
      solixpost-redis:
        condition: service_healthy
      solixpost-minio:
        condition: service_started
    networks:
      - solixpost-network

  # ─── PostgreSQL ──────────────────────────────────────────────────────────────
  solixpost-postgres:
    image: postgres:16-alpine
    container_name: solixpost-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-solixpost}
    volumes:
      - solixpost-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-solixpost}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - solixpost-network

  # ─── Redis ───────────────────────────────────────────────────────────────────
  solixpost-redis:
    image: redis:7-alpine
    container_name: solixpost-redis
    restart: unless-stopped
    volumes:
      - solixpost-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - solixpost-network

  # ─── MinIO ───────────────────────────────────────────────────────────────────
  solixpost-minio:
    image: minio/minio:RELEASE.2024-01-16T16-07-38Z
    container_name: solixpost-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
    command: server /data --console-address ":9001"
    volumes:
      - solixpost-minio-data:/data
    # Console de administração (opcional — remova se não precisar de acesso externo)
    ports:
      - "9001:9001"
    networks:
      - solixpost-network

networks:
  solixpost-network:
    driver: bridge

volumes:
  solixpost-postgres-data:
  solixpost-redis-data:
  solixpost-minio-data:
